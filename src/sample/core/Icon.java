// Icons represent things that the client would like to show to the
// player.

package sample.core;

public enum Icon {
    EMPTY,
    WALL,
    END,
    HERO,
    FOOTPRINT,
    MONSTER,
    TREASURE
}
